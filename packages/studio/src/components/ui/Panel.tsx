"use client";
import { Box, styled } from "@mui/material";

const Panel = styled(Box)({
    maxWidth: '300px',
    minWidth: '300px',
    height: '100%',
    overflowY: 'auto',
});

export default Panel;