type CanvasProps = {
    
};

export default function Canvas({}: CanvasProps) {
    return (
        <div>
            {/* Canvas content goes here */}
            <h1>Canvas</h1>
        </div>
    );
}
