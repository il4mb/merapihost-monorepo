"use client";
import { Button } from "@mui/material";

type LoginGoogleButtonProps = {

};

export default function LoginGoogleButton({ }: LoginGoogleButtonProps) {
    return (
        <Button variant="contained">
            Login with Google
        </Button>
    );
}
